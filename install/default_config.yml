app:
  name: Hikari Server Launcher
  host: 127.0.0.1
  port: 5000
  flask-debug: false
auth:
  enabled: true
  admin-key: "PLACEHOLDER"
workspace:
  path: "workspace"
server:
  java:
    auto_download: true
tui:
  enabled: false
backup:
  dir: "backups"
modpack:
  # 在线时使用的 UTF-8 不兼容规则；离线时不启用该功能。
  incompatible-list-url: "https://hsl-config.oss-cn-beijing.aliyuncs.com/incompatible.txt"
