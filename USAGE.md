# 启动方式

## Windows

安装后直接点击图标启动。

---

# 引导

1. 默认地址为 `http://127.0.0.1:5000`，无需修改。
2. 启动后会弹出两个窗口和一个App，其中一个窗口可以直接按任意键关闭，另一个窗口向上滑动可以找到管理秘钥（在一个黄框里），在引导中进行粘贴即可。
3. 如果位于中国大陆境内，可以点击优先选择镜像源。
4. 可以随时在设置页面切换镜像源。
